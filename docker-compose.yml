version: "3"
services:
  app:
    image: practice-web-server:latest
    networks:
      - net
    environment:
      - "MY_VAR=${MY_VAR}"
    command: sh scripts/start.sh
    ports:
      - 30000:8000
networks:
  net:

